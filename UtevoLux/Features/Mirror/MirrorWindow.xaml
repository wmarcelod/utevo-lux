<Window x:Class="UtevoLux.Features.Mirror.MirrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Utevo Lux - Espelho"
        Width="400" Height="300"
        MinWidth="80" MinHeight="60"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True"
        Background="#FF101010">

    <!-- AllowsTransparency=False is REQUIRED: DWM live thumbnails do not composite into WPF
         per-pixel-alpha (layered) windows. WindowChrome gives edge-resize without a caption. -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="RootBorder"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="2"
            Background="#FF101010"
            MouseLeftButtonDown="OnBodyMouseLeftButtonDown">
        <Grid x:Name="Host" Background="Black"/>
    </Border>
</Window>
